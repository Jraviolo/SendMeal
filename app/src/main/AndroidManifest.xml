<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tp.sendmeal">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".AltaPlato"></activity>
        <activity android:name=".CrearItem" android:parentActivityName=".Home"/>
        <activity android:name=".AltaPedido"  android:parentActivityName=".Home"/>
        <activity android:name=".MainActivity"    android:parentActivityName=".Home"/>

        <activity android:name=".ListaPlatos"  android:parentActivityName=".Home"/>

        <activity android:name=".Mapa" />
        <activity android:name=".MapaPedidos" android:parentActivityName=".Home"/>

        <activity android:name=".EditarPlato"/>

        <activity android:name=".BuscarPlato" android:parentActivityName=".AltaPedido"/>

        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<meta-data android:name="com.google.android.gms.version"
            android:value="49"
            tools:replace = "android:value"/>-->
<service android:name=".ComunicacionPush"
    android:exported="true">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGIN_EVENT" />
    </intent-filter>
</service>
        <service android:name=".FirebaseIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCuYHwH_F4qzlVLJYDJWIR5FZWS_eN5loA"/>

        <receiver android:name=".OfertaReceiver" android:exported="true">
            <intent-filter>

                <action android:name="SendMeal.EVENTO1_MSG"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>